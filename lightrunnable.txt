 Runnable lightRunnable = new Runnable() {
            public void run()
            {
                if (light.getZ() < 20 && lightFlag == false)
                {
                    light.setZ(light.getZ() + 2);
                }
                else
                {
                    lightFlag = true;
                    light.setZ(light.getZ() - 2);
                    if (light.getZ() == 10){lightFlag=false;}

                }
            }
        };

        executor = Executors.newScheduledThreadPool(1);

      ScheduledFuture<?> future =  executor.scheduleAtFixedRate(lightRunnable, 0, 50, TimeUnit.MILLISECONDS);